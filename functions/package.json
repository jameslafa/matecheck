{
  "name": "matecheck-webhook",
  "version": "1.0.0",
  "description": "Telegram webhook for MateCheck snooze buttons",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "test": "jest"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.5"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "globals": {
      "ts-jest": {
        "tsconfig": "tsconfig.test.json"
      }
    }
  }
}
